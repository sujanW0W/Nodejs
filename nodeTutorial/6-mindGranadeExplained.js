
require('./5-mindGranade');

console.log('Executed.')

//Here, the mindGranade module is imported. As we can see, the function in that module is executed.
//This means, when importing a module, the module gets executed.

//The 'require' reads the module, executes the module and finally returns the exports object.